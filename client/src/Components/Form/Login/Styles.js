import { makeStyles } from '@material-ui/core/styles'

export default makeStyles ((theme) => ({
    root: {
        marginTop: 100
    },
    container: {
        padding: 10,
    },
    button: {
        marginTop: 20
    },
    button2: {
        marginTop: 20,
        color: '#303F9F'
    },
    form: {
        paddingTop: 30
    }
}))